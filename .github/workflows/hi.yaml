name: Example

on:
  push:
    branches:
      - main

jobs:
  message:
    runs-on: ubuntu-latest
   
    steps: [
        {
          "name": "Checkout repository",
          "uses": "actions/checkout@v7"
        },
        {
          "name": "Print readme",
          "run": "cat"
        }
      ]